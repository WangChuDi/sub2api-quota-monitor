[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sub2api-quota-monitor"
version = "0.1.0"
description = "A cached Sub2API quota monitor with history charts and Bark alerts."
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
